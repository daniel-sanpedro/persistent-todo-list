let items = [];

const itemsDiv = document.getElementById("items");
// load items
function loadItems() {}

// render items
function renderitems() {
  // this clears the div and rewrites it
  itemsDiv.innerHTML = null;
  // [inx, itm]-
  for (const [idx, item] of Object.entries(items)) {

    
  }
}

// save items
function saveItems() {}

// add item
function addItem() {}

// remove item
function removeItem() {}
